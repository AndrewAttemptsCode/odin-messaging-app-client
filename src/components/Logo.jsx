import styled from "styled-components";

const Container = styled.div`
  color: blueviolet;
  font-size: 1.5rem;
  font-weight: bold;
`

const Logo = () => {
  return (
    <Container>
      Messenger
    </Container>
  );
};

export default Logo;