import styled from "styled-components";

const Container = styled.div`
  
`

const AccountStatus = () => {
  return (
    <Container>
      logged in name and logout button
    </Container>
  );
};

export default AccountStatus;